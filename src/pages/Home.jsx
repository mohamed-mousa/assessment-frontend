import { TopNavBar, NavBar, Footer } from "@/components";

const Home = () => {
  return (
    <main>
      {/* headers */}
      <TopNavBar />
      <NavBar />

      {/* footer */}
      <Footer />
    </main>
  );
};

export default Home;
